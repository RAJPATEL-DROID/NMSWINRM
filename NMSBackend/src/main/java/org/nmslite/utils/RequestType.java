package org.nmslite.utils;

public enum RequestType
{
    CREDENTIAL,
    DISCOVERY,
    VALID_DISCOVERY,
    DISCOVERY_RUN,
    DISCOVERY_RUN_RESULT,
    PROVISION,
    POLLING,
    POLLING_RESULT,
}
